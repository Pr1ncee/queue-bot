from pathlib import Path

db_queue = Path("..") / "databases" / "queueBot_db.pickle"  # The path to the queue database
db_users = Path("..") / "databases" / "users.pickle"  # The path to the users database
